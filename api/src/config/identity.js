/**
 * Identity constants for the BFHL challenge response.
 * Single source of truth — imported by processor and route handler.
 * Do NOT duplicate these values anywhere else in the codebase.
 */
const IDENTITY = Object.freeze({
  user_id: 'aditya_nair_101052005',
  email_id: 'an9103@srmist.edu.in',
  college_roll_number: 'RA2311026010257',
});

export default IDENTITY;
